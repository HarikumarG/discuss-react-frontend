import Routes from '../router';
const { Component } = require("react");

class App extends Component {
    render() {
        return (
            <Routes />
        )
    }
}

export default App;